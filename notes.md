Add a Combined String type to aggregate string output before printing. Ideally
this will allow a central debug type to the code. Doing it in tools bc its not
specific to the ECS

Add way to print what components are active on a given entity
Add a way to print all the value for active components on a given entity
Add a way to print a specific component on an active entity
Add game time so everything works by dt
Add way to apply force
Add way to apply times forces
